% =============================================================
% Ambiguity Report — LaTeX preamble (article class)
% Generated by ambiguity-report skill.
% Compile with: latexmk -pdf <file>.tex
% Or:           pdflatex <file>.tex (twice for cross-refs)
% =============================================================

\documentclass[11pt,letterpaper]{article}

% --- Geometry ---
\usepackage[margin=1in,top=1in,bottom=1in]{geometry}

% --- Encoding & language ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% --- Typography ---
\usepackage{microtype}
\linespread{1.05}

% --- Colors (placeholders — generator replaces these) ---
\usepackage{xcolor}
\definecolor{accentColor}{HTML}{8B3A1F}
\definecolor{accentSoft}{HTML}{C47E5A}
\definecolor{accentDeep}{HTML}{6B2D18}
\definecolor{deptColor}{HTML}{2C4A6E}
\definecolor{deptBg}{HTML}{EEF3F9}
\definecolor{advColor}{HTML}{8B3A1F}
\definecolor{advBg}{HTML}{FBF0EA}
\definecolor{bgColor}{HTML}{FBF8F1}
\definecolor{panelColor}{HTML}{F3EDE0}
\definecolor{cardColor}{HTML}{FFFFFF}
\definecolor{textColor}{HTML}{1F1B16}
\definecolor{textSoft}{HTML}{5B524A}
\definecolor{textFaint}{HTML}{8A8276}
\definecolor{borderColor}{HTML}{E3DCCA}
\definecolor{redraftBg}{HTML}{F7F2E3}
\definecolor{redraftBorder}{HTML}{E8DFBE}
\definecolor{highlightColor}{HTML}{FDF2C4}

% --- tcolorbox for the visual panels ---
\usepackage[most]{tcolorbox}
\tcbset{
  situationbox/.style={
    enhanced,
    colback=panelColor, colframe=accentSoft,
    boxrule=0pt, leftrule=2pt, arc=2pt,
    left=12pt, right=12pt, top=10pt, bottom=10pt,
    fontupper=\normalsize\itshape,
    before upper={\textsc{\footnotesize\bfseries\color{textFaint}The situation\\[4pt]}}
  },
  positionA/.style={
    enhanced,
    colback=deptBg, colframe=deptColor,
    boxrule=0pt, toprule=2pt, arc=2pt,
    left=12pt, right=12pt, top=10pt, bottom=10pt,
  },
  positionB/.style={
    enhanced,
    colback=advBg, colframe=advColor,
    boxrule=0pt, toprule=2pt, arc=2pt,
    left=12pt, right=12pt, top=10pt, bottom=10pt,
  },
  redraftbox/.style={
    enhanced,
    colback=redraftBg, colframe=redraftBorder,
    boxrule=0.5pt, arc=2pt,
    left=12pt, right=12pt, top=10pt, bottom=10pt,
    fontupper=\normalsize\itshape,
  },
  excerptbox/.style={
    enhanced,
    colback=cardColor, colframe=accentColor,
    boxrule=0pt, leftrule=3pt, arc=2pt,
    left=12pt, right=12pt, top=8pt, bottom=8pt,
  }
}

% --- Hyperref last ---
\usepackage[colorlinks=true,linkcolor=accentDeep,urlcolor=accentDeep,citecolor=accentDeep]{hyperref}

% --- Heading customization ---
\usepackage{titlesec}
\titleformat*{\section}{\Large\bfseries\color{textColor}}
\titleformat*{\subsection}{\large\bfseries\color{textColor}}
\titleformat*{\subsubsection}{\normalsize\bfseries\color{accentColor}}

% --- List customization ---
\usepackage{enumitem}
\setlist[description]{style=nextline, leftmargin=*}

% --- Margin notes ---
\usepackage{marginnote}

% --- Page color ---
\pagecolor{bgColor}
\color{textColor}

% --- Custom commands ---
\newcommand{\kicker}[1]{{\footnotesize\textsc{\color{accentColor}#1}}}
\newcommand{\anchorpill}[1]{\textcolor{accentColor}{\fbox{\footnotesize\textsf{§\,#1}}}}
\newcommand{\familypill}[2]{{\footnotesize\textsf{\textcolor{#2}{[#1]}}}}
\newcommand{\actor}[1]{{\scshape\small\color{textFaint}#1}}
\newcommand{\sideheaderA}[1]{{\footnotesize\textsf{\textbf{\color{deptColor}Position A} — \textit{#1}}}\\[4pt]}
\newcommand{\sideheaderB}[1]{{\footnotesize\textsf{\textbf{\color{advColor}Position B} — \textit{#1}}}\\[4pt]}
\newcommand{\redraftlabel}{{\footnotesize\textsc{\bfseries\color{accentColor}Proposed amendment}\\[4pt]}}
\newcommand{\weakpoint}[1]{\noindent\textbf{Weak point.}\quad #1\par\medskip}
\newcommand{\likelyoutcome}[1]{\noindent\textbf{Likely outcome.}\quad #1\par\medskip}

% --- Title metadata (generator fills these in) ---
% \title, \author, \date follow in the main file
